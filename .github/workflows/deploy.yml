name: Build and Deploy to Cloudflare Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Test
        run: npm test -- --run

      - name: Build
        # Publishable client-side keys: shipped in the JS bundle by design, not secrets.
        env:
          VITE_SUPABASE_URL: https://zdbjizfxkhdmwsotuzxh.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_yLvgmk7EbpSvpx2UcPstiw_BD4IH-IO
        run: npm run build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=tavernboard
